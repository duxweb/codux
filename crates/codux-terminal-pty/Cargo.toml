[package]
name = "codux-terminal-pty"
version = "1.8.1"
edition = "2024"

[dependencies]
codux-terminal-core = { path = "../codux-terminal-core" }
portable-pty = "0.9"
parking_lot = "0.12"
uuid = { version = "1", features = ["v4"] }
